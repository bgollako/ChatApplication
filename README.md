# ChatApp

A simple TCP based broadcasting chat application that allows users to chat with each other.

Compile the binary
```sh
go build .
```

Run the server
```sh
./chatapp --mode=server
```


Start client(s) :- 
```sh
./chatapp --mode=client
```

